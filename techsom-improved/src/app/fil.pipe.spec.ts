/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilPipe } from './fil.pipe';

describe('FilPipe', () => {
  it('create an instance', () => {
    const pipe = new FilPipe();
    expect(pipe).toBeTruthy();
  });
});
